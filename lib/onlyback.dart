import 'package:flutter/material.dart';

class OnlyBack extends StatelessWidget{
  @override
  Widget build(BuildContext context) {
    // TODO: implement build
    return Container(
      height: 500.0,
      color: Color(0xff00336A),
    );
  }

}